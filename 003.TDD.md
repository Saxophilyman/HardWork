Не поленился перечитать материал вводного занятия по TDD с курса карьеры. Хотя методика якобы понятна, меня развеселило, что в конечном итоге я столкнулся со всеми описанными проблемами, которые могут возникать при разработке по TDD. И реальные краевые случаи(возможные инварианты) и вопросы связности и ситуация, где ты сидишь, чтобы понять, что должен делать тестируемый код. Для меня всегда воспринималось отдельной головной болью и преградой для регулярного использования тестов то, что при изменении кода, нужно заново писать тесты. Соответственно пожалуй главный принципиальный вопрос будет заключаться в проектировании и определении спецификации. Действительно, а о чём же нам нужно подумать, чтобы правильно написать "рабочий код"? И здесь начинается главная сложность.

Мне всегда сложно определиться с чем мне лучше всего работать. Но по примеру автопарка и закрепления навыков из проекта попробовал реализовать идею книжного магазина. Во-первых, мне так и не удалась задача по луковичной архитектуре, в которой сперва определяется чистый доменный уровень. Во-вторых, для меня одной из любопытных тем показалась пагинация и сортировка. Даже не столько они, сколько создание спецификаций и мне хотелось бы немного более вручную разобраться с ними.  
Для начала определил простую сущность "книга". Если исходить под TDD, то ещё до прописки класса и классов с ней связанных, необходимо предположить тест для её функционала. И это взрывает мышление полностью. Контракт. Что мы проверяем? Что должно входить? Как нужно думать о том, чего ещё нет? А когда сделал минимум, а функционал расширяется, как сделать, чтобы это стало "проектно"? Я не буду прописывать все свои мыслительные цепочки и попытки итераций)) Но это оказалось не просто и пришлось уйти в ещё большее "обычное на пальцах", убрав весь Spring, оставшись с очень простой моделью. Также большое время заняло и просто написание, а вернее переписывание, в итерациях обычного кода.

По итогу первого пункта получились следующее:
```java
package ru.saxophilyman.book;
import org.junit.jupiter.api.Test;
import java.util.List;
import static org.assertj.core.api.Assertions.assertThat;

class BooksSearchTest {

    @Test
    void sortOnlyByRatingDesc() {

        List<Book> catalog = List.of(
                Book.of("1", "Книга1", 4.0, 1000),
                Book.of("2", "Книга2", 4.9, 1000),
                Book.of("3", "Книга3", 3.5, 1100)
        );

        CatalogSearch catalogSearch = new CatalogSearch();
        SearchQuery query = SearchQuery.empty()
                .withSort(SortVar.RATING_DESC);

        SearchResult result = catalogSearch.search(catalog, query);

        assertThat(result.total()).isEqualTo(3);
        assertThat(result.books()).extracting(Book::id)
                .containsExactly("2", "1", "3");
    }

    @Test
    void sortOnlyByRatingAsc(){
        List<Book> catalog = List.of(
                Book.of("1", "Книга1", 4.0, 1000),
                Book.of("2", "Книга2", 4.9, 1000),
                Book.of("3", "Книга3", 3.5, 1100)
        );

        CatalogSearch catalogSearch = new CatalogSearch();
        SearchQuery query = SearchQuery.empty()
                .withSort(SortVar.RATING_ASC);

        SearchResult result = catalogSearch.search(catalog, query);

        assertThat(result.total()).isEqualTo(3);
        assertThat(result.books()).extracting(Book::id)
                .containsExactly("3", "1", "2");
    }

    @Test
    void textSearchOfBooks() {
        List<Book> catalog = List.of(
                Book.of("1", "Граф Монте-Кристо", 4.0, 1000),
                Book.of("2", "Граф Калиостро", 4.9, 1100),
                Book.of("3", "Три Мушкетёра", 3.5, 1100)
        );

        CatalogSearch catalogSearch = new CatalogSearch();
        SearchQuery query = SearchQuery.empty()
                .withText("Граф")
                .withSort(SortVar.RATING_DESC);

        SearchResult result = catalogSearch.search(catalog,query);

        assertThat(result.total()).isEqualTo(2);
        assertThat(result.books()).extracting(Book::id)
                .containsExactly("2", "1");
    }

    @Test
    void sortOnlyByTitleAsc(){
        List<Book> catalog = List.of(
                Book.of("1", "Граф Монте-Кристо", 4.0, 1000),
                Book.of("2", "Граф Калиостро", 4.9, 1100),
                Book.of("3", "Три Мушкетёра", 3.5, 1100)
        );
        CatalogSearch catalogSearch = new CatalogSearch();
        SearchQuery query = SearchQuery.empty()
                .withSort(SortVar.TITLE_ASC);

        SearchResult result = catalogSearch.search(catalog, query);

        assertThat(result.total()).isEqualTo(3);
        assertThat(result.books()).extracting(Book::id)
                .containsExactly("2", "1", "3");
    }

    @Test
    void sortOnlyByTitleDesc() {
        List<Book> catalog = List.of(
                Book.of("1", "Граф Монте-Кристо", 4.0, 1000),
                Book.of("2", "Граф Калиостро", 4.9, 1100),
                Book.of("3", "Три Мушкетёра", 3.5, 1100)
        );

        CatalogSearch catalogSearch = new CatalogSearch();
        SearchQuery query = SearchQuery.empty()
                .withSort(SortVar.TITLE_DESC);

        SearchResult result = catalogSearch.search(catalog, query);

        assertThat(result.books()).extracting(Book::id)
                .containsExactly("3", "1", "2");
    }

    @Test
    void sortOnlyByPriceAsc() {
        List<Book> catalog = List.of(
                Book.of("1", "A", 4.0, 500),
                Book.of("2", "B", 4.9, 300),
                Book.of("3", "C", 3.5, 700)
        );

        CatalogSearch catalogSearch = new CatalogSearch();
        SearchQuery query = SearchQuery.empty()
                .withSort(SortVar.PRICE_ASC);

        SearchResult result = catalogSearch.search(catalog, query);

        assertThat(result.books()).extracting(Book::id)
                .containsExactly("2", "1", "3");
    }

    @Test
    void sortOnlyByPriceDesc() {
        List<Book> catalog = List.of(
                Book.of("1", "A", 4.0, 500),
                Book.of("2", "B", 4.9, 300),
                Book.of("3", "C", 3.5, 700)
        );

        CatalogSearch catalogSearch = new CatalogSearch();
        SearchQuery query = SearchQuery.empty()
                .withSort(SortVar.PRICE_DESC);

        SearchResult result = catalogSearch.search(catalog, query);

        assertThat(result.books()).extracting(Book::id)
                .containsExactly("3", "1", "2");
    }

    @Test
    void filterOnlyByMinRating_and_SortsByRatingDesc() {

        List<Book> catalog = List.of(
                Book.of("1", "A", 4.0, 500),
                Book.of("2", "B", 4.9, 700),
                Book.of("3", "C", 3.5, 300),
                Book.of("4", "D", 4.0, 200)
        );

        CatalogSearch catalogSearch = new CatalogSearch();

        SearchQuery query = SearchQuery.empty()
                .withMinRating(4.0)
                .withSort(SortVar.RATING_DESC);

        SearchResult result = catalogSearch.search(catalog, query);

        assertThat(result.total()).isEqualTo(3);
        assertThat(result.books()).extracting(Book::id)
                .containsExactly("2", "1", "4");
    }

    @Test
    void filterOnlyByMinPrice_and_SortsByPriceAsc() {

        List<Book> catalog = List.of(
                Book.of("1", "Граф Монте-Кристо", 4.0, 500),
                Book.of("2", "Граф Калиостро", 4.9, 300),
                Book.of("3", "Три Мушкетёра", 3.5, 700),
                Book.of("4", "Графиня де Монсоро", 4.2, 500)
        );

        CatalogSearch catalogSearch = new CatalogSearch();

        SearchQuery query = SearchQuery.empty()
                .withMinPrice(500)
                .withSort(SortVar.PRICE_ASC);

        SearchResult result = catalogSearch.search(catalog, query);

        assertThat(result.total()).isEqualTo(3);
        assertThat(result.books()).extracting(Book::id)
                .containsExactly("1", "4", "3");
    }

    @Test
    void filterOnlyByMaxPrice_and_SortsByPriceAsc() {

        List<Book> catalog = List.of(
                Book.of("1", "Граф Монте-Кристо", 4.0, 500),
                Book.of("2", "Граф Калиостро", 4.9, 300),
                Book.of("3", "Три Мушкетёра", 3.5, 700),
                Book.of("4", "Графиня де Монсоро", 4.2, 500)
        );

        CatalogSearch catalogSearch = new CatalogSearch();

        SearchQuery query = SearchQuery.empty()
                .withMaxPrice(500)
                .withSort(SortVar.PRICE_ASC);

        SearchResult result = catalogSearch.search(catalog, query);

        assertThat(result.total()).isEqualTo(3);
        assertThat(result.books()).extracting(Book::id)
                .containsExactly("2", "1", "4");
    }
}
```
Не знаю стоило ли сам код присылать, т.к. и так получилось достаточно много.
Казалось бы здесь много однотиных простых тестов, но последовательно они очень помогали при построении самого кода.  
И я бы не сказал, что использование TDD ускоряет разработку, по крайней мере для меня на данном этапе. Наоборот кажется, что возишься на одном месте. И обычный подход кажется ещё соблазнительным. Казалось бы - лепи всё что хочешь, после накинем сами тесты. Но вот необходимость подумать с позиции проектирования... Определить, что действительно и зачем это нужно и как выстраивать дальше. Сложно, но интересно. Акцент же на минимальной единице кода несколько упрощает "сложность" работы.  
Также удобством считаю сам итерационный принцип, где тесты пишутся не сразу рабочими, а наживляются на основной каркас. Но наверное в этом случае не стоит забывать про рефакторинг самого кода, потому что иногда я испытывал соблазн дописав тесты как "условно рабочие" оставить код в прежнем виде. Но дополнение функциональности призывало к "модульным" решениям.

Прочитав материалы, испытывал странное ощущение. Это именно о том, что меня внутри и задевало. Границы, определения, чёткости уровней. Сама концепция подхода. Даже сейчас я не смогу системно выразить свою мысль об этом и уж наверняка ошибусь при построении кода. Но всё же. Они стали более чётскими. Я уже знакомился с материалом "Три уровня рассуждений о программной системе", но в связке с практическим заданием он воспринимается ещё более серьёзнее. И мне кажется там много моментов, которые можно отдельно "пожевать". Другой вопрос, а как этому правильно следовать, ведь можно и "надумать" себе что-то.

Хорошо, придерживаясь "Код не должен следовать тестам, ни тесты не должны следовать коду. И тесты, и код должны следовать дизайну, логической архитектуре." на третьем пункте, в продолжении получилось следующее.

```java
    @Test
    void sortByRatingDescFirst_and_TitleAscSecond() {
        List<Book> catalog = List.of(
                Book.of("1", "A", 4.0, 1000),
                Book.of("2", "B", 4.0, 1000),
                Book.of("3", "C", 4.0, 1000),
                Book.of("4", "D", 4.9, 1000)
        );

        CatalogSearch catalogSearch = new CatalogSearch();
        SearchQuery query = SearchQuery.empty().withSort(SortVar.RATING_DESC);

        SearchResult result = catalogSearch.search(catalog, query);


        assertThat(result.books()).extracting(Book::id)
                .containsExactly("4", "1", "2", "3");
    }

    @Test
    void pag_returnsFirstPage() {
        List<Book> catalog = List.of(
                Book.of("1", "B", 4.0, 1000),
                Book.of("2", "A", 4.0, 1000),
                Book.of("3", "D", 4.9, 1000),
                Book.of("4", "C", 4.0, 1000),
                Book.of("5", "E", 3.5, 1000)
        );

        CatalogSearch catalogSearch = new CatalogSearch();
        SearchQuery query = SearchQuery.empty()
                .withSort(SortVar.RATING_DESC)
                .withSize(2)
                .withPage(0);

        SearchResult result = catalogSearch.search(catalog, query);

        assertThat(result.total()).isEqualTo(5);
        assertThat(result.books()).extracting(Book::id)
                .containsExactly("3", "2");
    }

    @Test
    void pag_returnsSecondPage() {
        List<Book> catalog = List.of(
                Book.of("1", "B", 4.0, 1000),
                Book.of("2", "A", 4.0, 1000),
                Book.of("3", "D", 4.9, 1000),
                Book.of("4", "C", 4.0, 1000),
                Book.of("5", "E", 3.5, 1000)
        );

        CatalogSearch catalogSearch = new CatalogSearch();
        SearchQuery query = SearchQuery.empty()
                .withSort(SortVar.RATING_DESC)
                .withSize(2)
                .withPage(1);

        SearchResult result = catalogSearch.search(catalog, query);

        assertThat(result.total()).isEqualTo(5);
        assertThat(result.books()).extracting(Book::id)
                .containsExactly("1", "4");
    }

    @Test
    void pag_returnsEmptyList_whenPageOutOfRange() {
        List<Book> catalog = List.of(
                Book.of("1", "B", 4.0, 1000),
                Book.of("2", "A", 4.0, 1000),
                Book.of("3", "D", 4.9, 1000),
                Book.of("4", "C", 4.0, 1000),
                Book.of("5", "E", 3.5, 1000)
        );

        CatalogSearch catalogSearch = new CatalogSearch();
        SearchQuery query = SearchQuery.empty()
                .withSort(SortVar.RATING_DESC)
                .withSize(2)
                .withPage(10);

        SearchResult result = catalogSearch.search(catalog, query);

        assertThat(result.total()).isEqualTo(5);
        assertThat(result.books()).isEmpty();
    }

    @Test
    void pag_whenSizeIsZero() {
        List<Book> catalog = List.of(
                Book.of("1", "A", 4.0, 100)
        );

        CatalogSearch catalogSearch = new CatalogSearch();
        SearchQuery query = SearchQuery.empty()
                .withSort(SortVar.RATING_DESC)
                .withSize(0)
                .withPage(0);

        assertThatThrownBy(() -> catalogSearch.search(catalog, query))
                .isInstanceOf(IllegalArgumentException.class)
                .hasMessageContaining("size");
    }

    @Test
    void pag_whenPageIsNegative() {
        List<Book> catalog = List.of(
                Book.of("1", "A", 4.0, 100)
        );

        CatalogSearch catalogSearch = new CatalogSearch();
        SearchQuery query = SearchQuery.empty()
                .withSort(SortVar.RATING_DESC)
                .withSize(10)
                .withPage(-1);

        assertThatThrownBy(() -> catalogSearch.search(catalog, query))
                .isInstanceOf(IllegalArgumentException.class)
                .hasMessageContaining("page");
    }

    @Test
    void priceCheck_whenMinPriceGreaterThanMaxPrice() {
        List<Book> catalog = List.of(
                Book.of("1", "A", 4.0, 100)
        );

        CatalogSearch catalogSearch = new CatalogSearch();
        SearchQuery query = SearchQuery.empty()
                .withMinPrice(500)
                .withMaxPrice(100);

        assertThatThrownBy(() -> catalogSearch.search(catalog, query))
                .isInstanceOf(IllegalArgumentException.class)
                .hasMessageContaining("minPrice")
                .hasMessageContaining("maxPrice");
    }

    @Test
    void ignoresNullBooksInCatalog() {
        List<Book> catalog = Arrays.asList(
                null,
                Book.of("1", "Alpha", 4.0, 100),
                null,
                Book.of("2", null, 5.0, 200)
        );

        CatalogSearch catalogSearch = new CatalogSearch();
        SearchQuery query = SearchQuery.empty().withSort(SortVar.RATING_DESC);

        SearchResult result = catalogSearch.search(catalog, query);

        assertThat(result.total()).isEqualTo(2);
        assertThat(result.books()).extracting(Book::id)
                .containsExactly("2", "1");
    }

    @Test
    void textSearch_Trims_and_Register() {
        List<Book> catalog = List.of(
                Book.of("1", "Граф Монте-Кристо", 4.0, 1000),
                Book.of("2", "граф Калиостро", 4.9, 1100),
                Book.of("3", "Три Мушкетёра", 3.5, 1100)
        );

        CatalogSearch catalogSearch = new CatalogSearch();
        SearchQuery query = SearchQuery.empty()
                .withText("   ГРАФ   ")
                .withSort(SortVar.RATING_DESC);

        SearchResult result = catalogSearch.search(catalog, query);

        assertThat(result.total()).isEqualTo(2);
        assertThat(result.books()).extracting(Book::id)
                .containsExactly("2", "1");
    }
```

Добавил саму пагинацию и проверку по её условным контрактам (надеюсь правильно)
Было бы интересно это проверить на уровне "любой компонент может быть заменён другой реализацией при сохранении спецификации"

Всё же на данном этапе это больше разовая акция, однако, если проводить параллель с первым заданием по ЦС, после которого почти каждый код я стал подвергать возможному, хотя бы элементарному, упрощению с точки зрения ЦС, потенциал данного подхода мне представляется очень интересным. Не только гипотетически, но получается уже и практически)
Не знаю насколько я прав, но подход к задачам с позиции грамотного проектирования мне представляется наиболее эффективным в плане освоения программирования. А TDD позволяет больше и главное осмысленнее упражняться именно в этом. Также приятно, что поработал над тестами, т.к. это одна из моих проблемных зон.
По факту я выделю следующую фразу, наиболее верно отражающую мою весь процесс работы над этим заданием.
"Вы скажете, что это очевидно, но это будет долго оставаться весьма расплывчатым и неоднозначным, пока вы по-настоящему не научитесь практическому пониманию того, как код получается просто следствием именно из дизайна, из строгой логики третьего уровня."
