Во-первых имеются сомнения правильно ли я понял задание.   
Простыми словами -  в случае повторяющейся "логики управления" имеет смысл выделить абстракцию и дать ей функцию для работы. 
Но что значит повторяющейся "логики управления"? То есть не просто код, а сценарий, связанный с определённой логикой. Но и над подобным мы уже работали...

Нашёл следующие варианты:  
№1  
Нахождение сущности в репозитории и принадлежности к предприятию  
Более мелкий, но универсальный пример:
```java
Vehicle vehicle = vehicleService.findOne(vehicleId);

if (vehicle.getEnterpriseOwnerOfVehicle() == null ||
        !Objects.equals(
                vehicle.getEnterpriseOwnerOfVehicle().getEnterpriseId(),
                enterpriseId
        )) {
    throw new AccessDeniedException("У вас нет доступа к этому автомобилю!");
}
```
Абстракция  
```java
Vehicle vehicle =
        vehicleService.findByIdAndEnterpriseOrThrow(
                vehicleId,
                enterpriseId
        );
```
Подойдёт как и для водителя, машины, предприятия и т.д.
```java
Vehicle vehicle = EntityFinder.entityFound(
        vehicleRepository.findById(vehicleId),
        "Машина не найдена"
);
```

Из этой же серии  
```java
private Vehicle getVehicleForEnterpriseOrThrow(Long enterpriseId, Long vehicleId) {
    Vehicle vehicle = vehicleService.findOne(vehicleId);

    if (vehicle.getEnterpriseOwnerOfVehicle() == null ||
            !Objects.equals(
                    vehicle.getEnterpriseOwnerOfVehicle().getEnterpriseId(),
                    enterpriseId
            )) {
        throw new AccessDeniedException("У вас нет доступа к этому автомобилю!");
    }

    return vehicle;
}
```
```java
public Vehicle findByIdAndEnterpriseOrThrow(Long vehicleId, Long enterpriseId) {
    return vehicleRepository
            .findByVehicleIdAndEnterpriseOwnerOfVehicle_EnterpriseId(
                    vehicleId,
                    enterpriseId
            )
            .orElseThrow(() -> new AccessDeniedException(
                    "У вас нет доступа к этому автомобилю!"
            ));
}
```
```java
private Vehicle getVehicleForEnterpriseOrThrow(Long enterpriseId, Long vehicleId) {
    return vehicleService.findByIdAndEnterpriseOrThrow(
            vehicleId,
            enterpriseId
    );
}
```



№2  
В расчёте периодов дня, месяца и года имеется почти одна и та же логика
```java
private List<ReportEntry> calculateMileageByDay(List<GpsPoint> gpsPoints, LocalDate startDate, LocalDate endDate) {
        Map<LocalDate, Double> dailyMileageMap = new HashMap<>();

        for (int i = 1; i < gpsPoints.size(); i++) {
            GpsPoint point1 = gpsPoints.get(i - 1);
            GpsPoint point2 = gpsPoints.get(i);

            LocalDate day = point1.getTimestamp().atZone(UTC).toLocalDate();
            double distance = calculateDistanceBetweenPoints(point1, point2);

            dailyMileageMap.put(day, dailyMileageMap.getOrDefault(day, 0.0) + distance);
        }

        List<ReportEntry> dailyReport = new ArrayList<>();
        for (LocalDate currentDay = startDate; !currentDay.isAfter(endDate); currentDay = currentDay.plusDays(1)) {
            double mileage = roundMileage(dailyMileageMap.getOrDefault(currentDay, 0.0));
            dailyReport.add(new ReportEntry(currentDay.toString(), mileage));
        }

        return dailyReport;
    }
```
Можно было бы попытаться вынести общую абстракцию для всех случаев, куда, возможно, и поместятся все управляющие моменты сценария
```java
private <K> List<ReportEntry> calculateMileageByPeriod(List<GpsPoint> gpsPoints, Iterable<K> periods, Function<GpsPoint, K> periodKeyExtractor, Function<K, String> periodLabelFormatter) {
    Map<K, Double> mileageByPeriod = new HashMap<>();

    for (int i = 1; i < gpsPoints.size(); i++) {
        GpsPoint point1 = gpsPoints.get(i - 1);
        GpsPoint point2 = gpsPoints.get(i);

        K periodKey = periodKeyExtractor.apply(point1);
        double distance = calculateDistanceBetweenPoints(point1, point2);

        mileageByPeriod.put(periodKey, mileageByPeriod.getOrDefault(periodKey, 0.0) + distance);
    }

    List<ReportEntry> reportEntries = new ArrayList<>();

    for (K period : periods) {
        double mileage = roundMileage(mileageByPeriod.getOrDefault(period, 0.0));
        reportEntries.add(new ReportEntry(periodLabelFormatter.apply(period), mileage));
    }

    return reportEntries;
}
```
тогда методы стали бы проще, а необходимые нюансы под период можно было бы реализовать в методах daysBetween/monthsBetween/yearsBetween
```java
private List<ReportEntry> calculateMileageByDay(List<GpsPoint> gpsPoints, LocalDate startDate, LocalDate endDate) {
    return calculateMileageByPeriod(
            gpsPoints,
            daysBetween(startDate, endDate),
            point -> point.getTimestamp().atZone(UTC).toLocalDate(),
            LocalDate::toString);
}
```
```java
private List<ReportEntry> calculateMileageByMonth(List<GpsPoint> gpsPoints, LocalDate startDate, LocalDate endDate) {
    return calculateMileageByPeriod(
            gpsPoints,
            monthsBetween(startDate, endDate),
            point -> YearMonth.from(point.getTimestamp().atZone(UTC).toLocalDate()),
            this::formatMonth);
}
```

№3  
У меня уже имеется более качественные версии методов для экспорта и импорта по формату файлов(особенно по сравнению с тем, что было изначально), но можно было бы попробовать уйти ещё выше по абстракции 
```java
private void importVehicleById(MultipartFile file) throws IOException {
    DataFormat dataFormat = DataFormat.fromFilename(file.getOriginalFilename());

    switch (dataFormat) {
        case JSON -> {
            VehicleExportDtoById dto = objectMapper.readValue(
                    file.getInputStream(),
                    VehicleExportDtoById.class
            );

            exportAndImportService.importFromDtoById(dto);
        }

        case CSV -> exportAndImportService.importFromCsv(file.getInputStream());
    }
}
```
```java
private void importVehicleByGuid(MultipartFile file) throws IOException {
    DataFormat dataFormat = DataFormat.fromFilename(file.getOriginalFilename());

    switch (dataFormat) {
        case JSON -> {
            VehicleExportDtoByGuid dto = objectMapper.readValue(
                    file.getInputStream(),
                    VehicleExportDtoByGuid.class
            );

            importServiceByGuid.importFromDtoByGuid(dto);
        }

        case CSV -> importServiceByGuid.importFromCsvGuid(file.getInputStream());
    }
}
```
Общая астракция
```java
private <T> void importByFormat(
        MultipartFile file,
        Class<T> jsonDtoClass,
        JsonImportAction<T> jsonImportAction,
        CsvImportAction csvImportAction
) throws IOException {

    DataFormat dataFormat = DataFormat.fromFilename(file.getOriginalFilename());

    switch (dataFormat) {
        case JSON -> {
            T dto = objectMapper.readValue(
                    file.getInputStream(),
                    jsonDtoClass
            );

            jsonImportAction.importJson(dto);
        }

        case CSV -> csvImportAction.importCsv(file.getInputStream());
    }
}
```
Функциональные интерфейсы для 
```java
@FunctionalInterface
private interface JsonImportAction<T> {
    void importJson(T dto);
}
```
```java
@FunctionalInterface
private interface CsvImportAction {
    void importCsv(InputStream inputStream) throws IOException;
}
```
```java
private void importVehicleById(MultipartFile file) throws IOException {
    importByFormat(
            file,
            VehicleExportDtoById.class,
            exportAndImportService::importFromDtoById,
            exportAndImportService::importFromCsv
    );
}
```
```java
private void importVehicleByGuid(MultipartFile file) throws IOException {
    importByFormat(
            file,
            VehicleExportDtoByGuid.class,
            importServiceByGuid::importFromDtoByGuid,
            importServiceByGuid::importFromCsvGuid
    );
}
```
